package Entities;

/** A Monster is a simple creature with less methods than character. */
public class Monster extends Creature {
    public Monster(String name, int strength, int craft, int health) {
        super(name, strength, craft, health);
    }
}
